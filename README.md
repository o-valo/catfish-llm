# catfish-llm
[ENG] A lightweight proxy service that intercepts LLM outputs, extracts valid UCI chess moves, and routes the actual calculation to Stockfish—stripping away model hallucinations and illegal moves before they hit the board.
